<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>

        <!-- Console appender: logs to stdout -->
        <Console name="Standard Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- File appender: logs to file with rolling policy -->
        <RollingFile name="File" fileName="logs/app.log"
                     filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>

    </Appenders>

    <Loggers>
        <!-- Root logger: catches everything at INFO level and above -->
        <Root level="info">
            <AppenderRef ref="Standard Console"/>
            <AppenderRef ref="File"/>
        </Root>

        <!-- Package-level logger for our app -->
        <Logger name="com.ms.credit" level="debug" additivity="false">
            <AppenderRef ref="Standard Console"/>
            <AppenderRef ref="File"/>
        </Logger>

    </Loggers>
</Configuration>
